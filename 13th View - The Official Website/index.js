// const wrapper = document.querySelector('.text-wrapper');

// // Clone the content to ensure smooth looping
// const clone = wrapper.cloneNode(true);
// wrapper.appendChild(clone);

// setTimeout(() => {
//     const clone = wrapper.cloneNode(true);
//     wrapper.appendChild(clone);
// }, 10000); // Adjust the delay as needed
